env: dev

rollingUpdate:
  maxSurge: 1
  maxUnavailable: 1
image:
  identifier: ${artifact.metadata.image}
  imagePullPolicy: Always

configmap:
  pubsub:
    credentials:
      path: file:///etc/ukur-secret/ukur-service-account.json

service:
  http:
    externalPort: 80
    internalPort: 8080
ingress:
  enabled: true
  devIp: 35.195.223.29
  stagingIp: 35.195.146.93
  productionIp: 35.187.15.14